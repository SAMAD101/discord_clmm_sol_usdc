import { Interaction } from "discord.js";
import { queue } from "../../queue";

export const openPositionController = async (interaction: Interaction) => {};

export const closePositionController = async (interaction: Interaction) => {};

export const fetchPositionController = async (interaction: Interaction) => {};
